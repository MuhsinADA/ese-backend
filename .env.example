# Core
DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=True
DATABASE_URL=postgres://localhost/ese_tasks
ALLOWED_HOSTS=localhost,127.0.0.1

# CORS — comma-separated origins allowed to call the API
CORS_ALLOWED_ORIGINS=http://localhost:5173

# Frontend URL (used in password-reset email links)
FRONTEND_BASE_URL=http://localhost:5173

# SendGrid (email delivery)
SENDGRID_API_KEY=
DEFAULT_FROM_EMAIL=noreply@example.com

# Cloudinary (profile image storage)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# ── Render production overrides (for reference) ──
# DJANGO_DEBUG=False
# DATABASE_URL=<auto-injected by Render PostgreSQL>
# ALLOWED_HOSTS=.onrender.com
# CORS_ALLOWED_ORIGINS=https://<frontend>.onrender.com
# FRONTEND_BASE_URL=https://<frontend>.onrender.com
